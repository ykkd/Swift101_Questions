import Foundation

// 新しい構造体「Point」を定義し、その中に「x」と「y」というInt型のプロパティを追加してください。

// 変数「point1」を宣言し、「Point」のインスタンスを作成して、xに10、yに20を設定してください。

// 変数「point2」を宣言し、「point1」を代入してください。次に、「point2」のxの値を30に変更してください。最後に「point1」のxの値を出力してください。

// 新しいクラス「Person」を定義し、その中に「name」というString型のプロパティを追加してください。

// 変数「person1」を宣言し、「Person」のインスタンスを作成して、nameに"John"を設定してください。

// 変数「person2」を宣言し、「person1」を代入してください。次に、「person2」のnameの値を"Jane"に変更してください。最後に「person1」のnameの値を出力してください。

// 新しい列挙型「Fruit」を定義し、その中に「apple」「banana」「cherry」というケースを追加してください。それぞれのケースに対応する文字列型のrawValueを設定してください。

/*
 列挙型「Direction」を定義し、その中に「north」「south」「east」「west」というケースを追加してください。それぞれのケースに対応する整数型のrawValueを設定してください。
 ただし、northは1,westは4になるように設定してください。
 さらに、rawValueを使用して初期化できる失敗可能イニシャライザを追加してください。
 */

/*
 新しい列挙型「ShapeType」を定義し、その中に「circle」「rectangle」「triangle」というケースを追加してください。
 以下の通り、それぞれのケースに対応する連想値を持たせてください。
 circle（円）は半径を表すDouble型、rectangle（四角形）は幅と高さを表すDouble型の変数二つ、triangle（三角形）は底辺と高さを表すDouble型の変数二つ。

 列挙型「ShapeType」に、各形状の面積を計算してDouble型の値を返すメソッド「area」を追加してください。
 */

// 列挙型「ShapeType」を活用して、半径の値が34の円の面積を出力してください
