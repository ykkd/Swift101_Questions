import Foundation

// 新しいプロトコル「Printable」を定義し、その中に「printDescription()」というメソッドを追加してください。

// プロトコル「Printable」にデフォルト実装を追加し、「印刷可能なドキュメントです」を出力するようにしてください。


/*
 クラス「MeetingReport」を定義し、「Printable」プロトコルに準拠させ、「title」と「content」というプロパティを追加してください。初期化メソッドも実装してください。
 関数「printDescription」を実行すると「タイトル: {title}, 内容: {content}」を出力するように実装しなさい
 */


/*
 新しいプロトコル「Drivable」を定義してください。
 その中に「maxSpeed」という最高速度を表すInt型のプロパティと、走行距離を示すInt型のプロパティ「drivingDistance」を追加してください。
 protocolにおけるgetter, setterについて自身で調べ、注意して指定してください。
 さらに「drive」という走行距離を意味するInt型の引数を取るメソッドを追加してください。
 */

/*
 クラス「Car」を定義し、「Drivable」プロトコルに準拠させてください。
 車の車種を意味する文字列型の定数「model」を追加し、イニシャライザを経由して最高速度と車種を指定できるようにしてください。
 drivingDistanceの初期値は0を指定してください。
 
 「{model}の車が走行中です。最高速度は時速約{maxSpeed}キロです。」と出力する関数「drive」を実装してください。
 また、driveの処理の最後に、drivingDistance(走行距離)を変数drivingDistanceに加算してください。

 */

/*
 定数myCarにCarクラスのインスタンスを代入してください。
 モデル名と最高速度はそれぞれ「ZVW50（トヨタプリウス）」「200」を指定してください。
 myCarのdrive関数し、任意の距離を走らせてください。
 */
