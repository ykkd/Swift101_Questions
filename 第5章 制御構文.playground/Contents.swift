import Foundation

// 変数「score」を宣言し、75を代入してください。その後、if文を使用して、scoreが60以上なら「合格」と出力し、そうでない場合は「不合格」と出力してください。

// 変数「optionalNumber」を宣言し、nilを代入してください。if-let文を使用して、optionalNumberに値がある場合はその値を出力し、ない場合は「値がありません」と出力してください。

// 変数「age」を宣言し、18を代入してください。引数なしの関数「checkAge」を宣言してください。関数内で、guard文を使用して、ageが20以上でなければ「年齢制限があります」と出力し、20以上であれば「ようこそ」と出力してください。最後に、checkAge関数を実行してください。

// 変数「fruit」を宣言し、「apple」を代入してください。switch文を使用して、fruitの値が「apple」の場合は「リンゴです」、fruitの値が「banana」の場合は「バナナです」、それ以外の場合は「その他の果物です」と出力してください。

// for-in文を使用して、1から5までの数値を順に出力してください。

// 変数「count」を宣言し、0を代入してください。while文を使用して、countが5未満の場合、countを出力し、その後countを1増加させてください。

// 変数「repeatCount」を宣言し、0を代入してください。repeat-while文を使用して、repeatCountが5未満の場合、repeatCountを出力し、その後repeatCountを1増加させてください。

// 配列「numbers」を宣言し、[1, 2, 3, 4, 5]を代入してください。for-in文を使用して、numbersの各要素を出力してください。

// 変数「i」を宣言し、1を代入してください。while文を使用して、iが10以下の場合にiを出力し、iが5になった場合にbreak文を使用してループを終了してください。

// 変数「j」を宣言し、0を代入してください。while文を使用して、jが10以下の場合にjを出力し、jが偶数の場合にはcontinue文を使用して次のループに進んでください。
